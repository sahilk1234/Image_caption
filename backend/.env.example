# DB: use SQLite for MVP; swap to Postgres by setting DATABASE_URL
DATABASE_URL=sqlite:///./imgcap.db

# JWT
JWT_SECRET=change-me
JWT_EXPIRE_MINUTES=43200  # 30 days

# Artifacts (relative to backend/)
MODEL_TS=artifacts/model_ts.pt
WEIGHTS_PT=artifacts/weights.pt
VOCAB_JSON=artifacts/vocab.json
CONFIG_JSON=artifacts/config.json

# Inference
IMG_SIZE=224